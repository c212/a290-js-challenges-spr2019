var numberList = [ 1, 2, 5, 7 ]; // Code will be tested with different arrays.
var processedValue = 0;

try {
   processedValue = processNumbers(numberList);
}
catch (exception) {
   processedValue = -1;
}
